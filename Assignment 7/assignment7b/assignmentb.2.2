#include <iostream>
using namespace std;
class bank
{
    string name, type_ac;
    int AC_no;
    float bal;
     string status = "Activate";
public:
    void input();
    void display();
    void depo();
    void withd();
    friend class bankmanager;
};
class bankmanager {
public:
    void showbalance(bank x);
    void reset(bank &x); // Modified function signature to take a reference to a bank object
};

void bankmanager::showbalance(bank x) { 
    int inputAC_no;
    cout << "Enter the account number:" << endl;
    cin >> inputAC_no;
    if (inputAC_no == x.AC_no) {
        cout << "Balance: " << x.bal;
    } else {
        cout << "Account not found." << endl;
    }
}

void bankmanager::reset(bank& x) {
    string a;
    cout << "Enter the account status (Activate/Deactive):" << endl;
    cin >> a;
    if (a == "Deactive") {
        x.status = "Deactivate"; 
        cout << "Account deactivated successfully." << endl;
    } else {
        cout << "Invalid status. Account remains active." << endl;
    }
}
void bank::input()
{
    cout << "Write the customer name: " << endl;
    cin >> name;
    cout << "Write the Account type: " << endl;
    cin >> type_ac;
    cout << "Write account no. :" << endl;
    cin >> AC_no;
    cout << "Write the balance:"<<endl;
    cin >> bal;
}

void bank::display()
{
    cout << "The customer name: " << endl;
    cout << name<<endl;
    cout << "The Account type: " << endl;
    cout << type_ac<<endl;
    cout << "The account no. :" << endl;
    cout << AC_no<<endl;
    cout << "The balance:"<<endl;
    cout << bal<<endl;
}

void bank::depo()
{
    int dep;
    cout << "Write the amount you Deposite: " << endl;
    cin >> dep;
    bal = bal + dep;
    cout << "The total amount is : " << bal<< endl;
}

void bank::withd()
{
    int wit;
    cout << "Write the amount to withadraw: " << endl;
    cin >> wit;
    bal = bal-wit;
    cout << "The amount present is: " << bal<<endl;
}
int main()
{
    bank b1;
    bankmanager b2;

    int ch;
    string choice;
    cout << "The choices number are as following: " << endl
         << "1:For Input." << endl
         << "2:For deposite money." << endl
         << "3:For withdraw money." << endl
         << "4:For Display Information."<<endl<<endl;
    do
    {
        cout << "Enter the choice: ";
        cin >> ch;
        switch (ch)
        {
        case 1:
            b1.input();
            break;

        case 2:
            b1.depo();
            break;

        case 3:
            b1.withd();
            break;

        case 4:
            b1.display();
            break;

        default:
            cout << "The choice selected by you is invalid.";
        }
        cout << "Do you wan't to continue again Yes/No:";
        cin >> choice;

    } while (choice == "Yes");
     
    
    cout<<"For bank manager:"<<endl;
    int choi;
    cout<<endl<<endl;
    cout<<"The choices number are as following: " << endl
        <<"1:For Showbalance."<<endl<<"2:For Reset Account:"<<endl;
     do
    {   cout<<endl;
        cout << "Enter the choice: ";
        cin >> choi;
        switch (choi)
        {
        case 1:
            b2.showbalance(b1);
            break;

        case 2:
            b2.reset(b1);
            break;

        default:
            cout << "The choice selected by you is invalid."<<endl;
        }
        
        cout << "Do you wan't to continue again Yes/No:";
        cin >> choice;

    } while (choice == "Yes");
     
    return 0;
}
